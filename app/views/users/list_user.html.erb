<div class="user-list">
    <div class="container">
        <div class="row">
            <% @users.each do |user| %>
            <%= render user %>
            <% end %>
        </div>
        <div class="d-flex flex-column align-items-center mt-3">
            <%= will_paginate @users%>
        </div>
    </div>
<div>